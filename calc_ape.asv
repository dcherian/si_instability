fname = 'E:\Work\instability\ROMS\si_part\edge\3D\run-ape\ocean_his.nc';

rho = ncread(fname,'rho',[1 1 1 1],[Inf Inf Inf 1]);

rgrid = roms_get_grid(fname,fname,0,1);

xr = rgrid.x_rho(1,:);
zr = rgrid.z_r(:,1,1);
dy = mean(diff(rgrid.y_rho(:,1)));

ape = ape(xr,zr,rho(:,1,:),